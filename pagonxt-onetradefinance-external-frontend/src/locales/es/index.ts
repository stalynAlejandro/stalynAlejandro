import comments from './comments.json';
import common from './common.json';
import forms from './forms.json';
import violations from './violations.json';

export default { comments, common, forms, violations };
